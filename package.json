{
  "name": "react-to-heroku",
  "version": "1.0.0",
  "description": "Automatically create and deploy a React app to Heroku.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jo Imlay / dentednerd",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.3.0",
    "shelljs": "^0.8.2"
  },
  "bin": {
    "react-to-heroku": "index.js"
  }
}
